# 数据结构

CANN 算子 API 依赖的基础数据结构，**开发者无需关注其内部实现，直接使用即可**。

通过[《算子加速库》](https://hiascend.com/document/redirect/CannCommercialOplist)中"公共接口"创建。

## 核心数据结构

| 数据结构 | 说明 | 创建接口 |
|----------|------|----------|
| `aclTensor` | 张量数据管理（向量、矩阵等） | `aclCreateTensor` |
| `aclScalar` | 标量数据管理（单一数值） | `aclCreateScalar` |
| `aclIntArray` | 整型数组结构 | `aclCreateIntArray` |
| `aclFloatArray` | float32 数组结构 | `aclCreateFloatArray` |
| `aclBoolArray` | 布尔数组结构 | `aclCreateBoolArray` |
| `aclTensorList` | 多变张量数组结构 | `aclCreateTensorList` |
| `aclScalarList` | 标量数组结构 | `aclCreateScalarList` |
| `aclOpExecutor` | 算子执行器（框架自动管理） | 自动创建/释放 |
| `aclrtStream` | 流处理器，管理异步操作执行顺序 | — |

## 类型定义

```cpp
typedef struct aclTensor aclTensor;
typedef struct aclScalar aclScalar;
typedef struct aclIntArray aclIntArray;
typedef struct aclFloatArray aclFloatArray;
typedef struct aclBoolArray aclBoolArray;
typedef struct aclTensorList aclTensorList;
typedef struct aclScalarList aclScalarList;
typedef struct aclOpExecutor aclOpExecutor;
typedef void *aclrtStream;
```

## aclOpExecutor 生命周期

- 调用一阶段接口 `aclxxXxxGetWorkspaceSize` 时 → 框架自动创建
- 调用二阶段接口 `aclxxXxx` 后 → 框架自动释放该对象

## 来源
- `ops-transformer_AI/docs/zh/context/数据结构.md`
