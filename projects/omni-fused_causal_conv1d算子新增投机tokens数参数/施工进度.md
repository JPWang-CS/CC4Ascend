# fused_causal_conv1d 投机 tokens 数扩展 — 施工进度

> 配套：`实施计划_投机tokens数扩展.md`（方案）/ `完整技术文档.md`（原理）/ `需求分析与设计说明书 - A3.md`（绝对需求，不改）
> 本文件：落地过程记录（改动点、编译/验证结果、问题），按阶段更新
> 起始：2026-08-03

## 决策基线（C1-C5 全定，详见实施计划 §五）

| 项 | 结论 | 状态 |
|---|---|---|
| C1 | MAX_DECODE_LEN=17 | ✅ |
| C2 | 不进 OpDef，走通道 B（graph setter 动态下发）| ✅ |
| C3 | 分层命名（aclnn/setter/TilingData camelCase；schema/局部 snake；INDEX 大写 snake）| ✅ |
| C4 | aclnn 追加 attrs 末尾（inplace 后、yOut 前）| ✅ |
| C5 | 路径 A/B 共存，只新增落点（`:245` 后放大 stateLen_）| ✅ |
| R1 | UB 自适应不溢出 | 🔬 待上板 |
| R2/R3 | 随 C5/C2 消解 | ✅ |

---

## 阶段 1：签名层（aclnn + OP_ATTR 末尾追加 + binding + schema）

- **状态**：代码已落，编译待 Linux 环境验证
- **负责**：ascendc-host-engineer
- **机制修正**：原计划 graph setter（C2）。host-engineer trace 发现 fused_causal_conv1d **无 graph 类**（aclnn 直调 l0op），且 matmul 的 `SetBatchInvariant` 最终也走 OP_ATTR（`matmul.cpp:45`）。用户确认改 **路径 B'**：l0op OP_ATTR 末尾追加 + tiling 防御读（不加 graph 类）
- **改动点**（已落）：
  - `op_api/ai_infra_fused_causal_conv1d.h:39` + `.cpp:44/58`（l0op 签名 + OP_ATTR 末尾追加 `maxDraftTokens`）
  - `op_api/aclnn_ai_infra_fused_causal_conv1d.h:42` + `.cpp:34/126/144/182`（aclnn 签名 + CommonProcess + 透传）
  - `torch_ops_extension/.../npu_ai_infra_fused_causal_conv1d.cpp:27/50/74/95`（npu+meta 双签名 + `value_or(7)` + EXEC_NPU_CMD_V1 透传）
  - `ops_def_registration.cpp:70`（schema m.def 加 `int max_draft_tokens=7`）
  - UT `tests/ut/op_api/test_aclnn_ai_infra_fused_causal_conv1d.cpp` 37 处加 `max_draft_tokens`
- **OP_ATTR 改前→改后**：`OP_ATTR(activationMode, padSlotId, runMode, maxQueryLen, residualConnection, blockSize, convMode, inplace)` → 末尾追加 `, maxDraftTokens`（index 8，**原 8 个顺序不动**）
- **未动**：def.cpp / OP_INPUT / OP_OUTPUT（不进 OpDef，符合"只新增"）
- **检查点**：静态核验通过（签名-调用计数对齐、UT 37 处对应、OP_ATTR 原 8 顺序不动）；**build.sh 未跑**（本机 Windows 无 CANN/cmake）
- **待用户跑**（Linux/Docker）：
  ```
  bash build.sh -n ai_infra_fused_causal_conv1d -c ascend910b
  bash build.sh -n ai_infra_fused_causal_conv1d -c ascend910b --ut
  ```
- **证据等级**：WEAK（静态核验通过，编译未验证）

## 阶段 2：tiling 层

- **状态**：待阶段 1
- **改动点**：H1 `MAX_M=16` / H2 `MAX_DECODE_LEN=17` / T1 防御读 maxDraftTokens / C5 落点（`:245` 后放大 stateLen_，不动 :237/:560/:564/:590/:709）

## 阶段 3：kernel 上板验证（R1）

- **状态**：待阶段 2
- **检查点**：msprof 跑 D=16384 × m=16 × W=6，UB < UB_SIZE + 性能可接受

## 阶段 4：golden + atk

- **状态**：G3 已落地（2026-08-03）；G1/G2 待 binding
- **负责**：ascendc-kernel-semantics-researcher（G3）

### G3 项目 golden（已落地）

- **脚本**：`projects\omni-fused_causal_conv1d算子新增投机tokens数参数\golden\golden_fused_causal_conv1d_mtp.py`
- **自检脚本**：同目录 `selfcheck_shape_offset.py`（纯 python 无依赖）
- **oracle 继承**：atk `causal_conv1d_golden`，逐行 trace 自
  - `executor_ai_infra_fused_causal_conv1d_continue.py:73-248`（decode 主用）
  - `executor_ai_infra_fused_causal_conv1d_pref.py:95-268`（prefill，语义等价，已逐行比对确认与 continue 版本一致）
  - numpy 版 `causal_conv1d_golden_np` + torch 版 `causal_conv1d_golden_torch` 双实现
- **双 oracle 互比**：atk 版（einsum 滑窗）vs 朴素版（显式滑窗循环 `naive_causal_conv1d_np`），走不同代码路径互比
- **caseData 矩阵**：96 cases = m∈{0,1,7,8,12,16} × K∈{3,6} × D∈{1024,16384} × accepted{0,1,m//2,m+1} × apc{on,off}
- **判据**：双标杆 ratio（max_re≤5, avg_re≤1.5, rms≤1.5），对齐 atk json `cv_fused_double_benchmark`；无 GPU 降级单标杆 10/2/2
- **输出**：y + conv_states 两份都比对（cache 写错不反映在 y 上是常见 false-pass）
- **自洽验证状态**：
  - 本机无 numpy/torch（离线开发机，pypi 超时），主脚本数值自洽**未跑通**（WEAK）
  - 已用纯 python 跑结构性自检（`selfcheck_shape_offset.py`）：84 cases 全 PASS，覆盖 m∈[0,16] × K∈{3,6} × apc × accepted 全边界的 shape/offset/cache 越界/apc boundary 约束
  - 主脚本 AST 语法检查 PASS
  - **结论**：oracle 逻辑结构性正确（WEAK，未跑数值）；数值正确性待有 numpy/torch 的机器跑 `python golden_fused_causal_conv1d_mtp.py` 确认
- **待 binding 落地后接入**：`--full` 模式调 `torch.ops.custom.npu_ai_infra_fused_causal_conv1d(..., maxDraftTokens=...)`，npu 输出 vs oracle ratio 比对

### G1/G2 atk

- **状态**：待阶段 1 binding 落地
- **改动点**：11 json 加 `maxDraftTokens` 字段 + 新增 m∈{8,12,16} case；11 executor 调用加 `maxDraftTokens=`

## 阶段 5：文档同步

- **状态**：待
- **改动点**：M1 aclnn doc / M2 torch doc / M3 上游 doc（视交付要求）
